# Fix for xdg-desktop-portal and GTK/Electron apps
# Exports Wayland environment variables to systemd user session
exec systemctl --user import-environment WAYLAND_DISPLAY SWAYSOCK
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP=sway
